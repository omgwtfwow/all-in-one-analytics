(function ($) {
	'use strict';
	$(function () {

		/*        console.log('alive');

				var tracks = tracksAIO.tracks;
				jQuery.each(tracks, render_track_calls);

				function render_track_calls(index, item) {

					console.log(item);
					//   console.log(item.properties);

					analytics.track(item.event);
					//  analytics.track(item.event, parseJSON(item.properties), parseJSON(item.options));

					if (!Cookies.get('aio_analytics_clear')) {
						console.log('clearing');
						Cookies.set('aio_analytics_clear', true);
					}


				}*/

	});

})(jQuery);